import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';  // CSS được import trực tiếp trong React
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

